# AI案例拆解平台

一个专业的AI项目案例分析平台，深入拆解AI项目的落地路径和盈利模式。

## 🎯 项目概述

本平台旨在帮助开发者和创业者理解AI项目的商业化路径，通过真实案例分析，提供从0到1的AI项目落地指导。

### 核心功能

- 📚 **案例库**: 精选AI项目案例，包含详细的落地过程和盈利数据
- 🔍 **智能搜索**: 支持关键词搜索和多维度筛选
- 🏷️ **分类标签**: 按类别、盈利模式、发展阶段分类
- 💼 **咨询服务**: 提供专业的AI项目落地咨询
- 🔖 **收藏功能**: 保存感兴趣的案例（待实现）

## 🛠️ 技术栈

- **框架**: Next.js 16.1.6 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS 3.4.17
- **UI组件**: shadcn/ui (Radix UI)
- **图标**: Lucide React
- **内容**: Markdown + Gray Matter
- **搜索**: FlexSearch
- **后端**: Supabase (可选)

## 📁 项目结构

```
ai_projects_analysis/
├── src/
│   ├── app/                    # Next.js App Router 页面
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 首页
│   │   ├── cases/              # 案例相关页面
│   │   │   ├── page.tsx        # 案例列表（服务器组件）
│   │   │   ├── CasesClient.tsx # 案例列表（客户端组件）
│   │   │   └── [slug]/         # 动态路由
│   │   │       └── page.tsx    # 案例详情
│   │   ├── bookmarks/          # 收藏页面
│   │   ├── consulting/         # 咨询页面
│   │   └── globals.css         # 全局样式
│   ├── components/             # React 组件
│   │   ├── Navbar.tsx          # 导航栏
│   │   ├── CaseCard.tsx        # 案例卡片
│   │   └── ui/                 # shadcn/ui 组件
│   ├── lib/                    # 工具函数
│   │   ├── content.ts          # 内容加载器
│   │   ├── search.ts           # 搜索功能
│   │   ├── supabase.ts         # Supabase 客户端
│   │   └── utils.ts            # 通用工具
│   └── types/                  # TypeScript 类型定义
│       └── index.ts
├── content/                    # Markdown 内容
│   └── cases/                  # 案例文件
│       ├── ai-customer-service-50k.md
│       ├── ai-writer-saas.md
│       └── ai-image-generator.md
├── public/                     # 静态资源
├── docs/                       # 文档
│   └── plans/                  # 实施计划
└── package.json
```

## 🚀 快速开始

### 环境要求

- Node.js 18.0 或更高版本
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 环境变量配置

创建 `.env.local` 文件：

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 开发模式

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

### 生产构建

```bash
npm run build
npm start
```

## 📝 添加新案例

在 `content/cases/` 目录下创建新的 Markdown 文件：

```markdown
---
title: "案例标题"
slug: "case-slug"
category: "分类"
monetization: "盈利模式"
stage: "发展阶段"
publishedAt: "2025-01-15"
tags: ["标签1", "标签2"]
cover: "/images/cases/cover.jpg"
---

## 项目背景

项目背景描述...

## 产品形态

产品形态描述...

## 落地过程

落地过程描述...

## 盈利模式

盈利模式描述...
```

## 🎨 设计系统

### 配色方案

- **主色调**: 蓝色系 (#1E3A8A, #3B82F6)
- **辅助色**: 绿色 (#10B981)、紫色 (#8B5CF6)
- **中性色**: 灰色系 (#64748B, #E2E8F0)

### 字体

- **标题**: Space Grotesk
- **正文**: DM Sans

### 布局风格

- **设计风格**: Swiss Modernism（瑞士现代主义）
- **布局**: Bento Grid（便当盒布局）
- **卡片**: 圆角、阴影、悬停效果

## 🔧 已解决的技术问题

### 1. Next.js 15+ Params 异步问题

**问题**: 动态路由中 `params` 现在是 Promise
**解决**: 使用 `await params` 解包

```typescript
export default async function Page({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  // ...
}
```

### 2. 服务器组件与客户端组件分离

**问题**: `fs` 模块无法在客户端使用
**解决**: 将数据获取放在服务器组件，交互逻辑放在客户端组件

```typescript
// page.tsx (服务器组件)
export default function CasesPage() {
  const cases = getAllCases();
  return <CasesClient initialCases={cases} />;
}

// CasesClient.tsx (客户端组件)
'use client';
export function CasesClient({ initialCases }) {
  // 客户端交互逻辑
}
```

### 3. CSS @import 顺序

**问题**: `@import` 必须在所有其他规则之前
**解决**: 将 Google Fonts 导入移到 CSS 文件顶部

### 4. Tailwind CSS 配置

**问题**: ES Modules 与 CommonJS 混用
**解决**: 在 `tailwind.config.ts` 中保持 `require()` 用法

## 📊 性能优化

- ✅ 静态生成 (SSG) 所有案例页面
- ✅ 图片占位符使用 CSS 渐变
- ✅ 客户端搜索和筛选
- ✅ 代码分割和懒加载

## 🔜 待实现功能

- [ ] 用户认证和登录
- [ ] 收藏功能（Supabase 集成）
- [ ] 评论系统
- [ ] 案例评分
- [ ] 高级搜索过滤
- [ ] 深色模式
- [ ] 国际化 (i18n)
- [ ] SEO 优化
- [ ] Analytics 集成

## 📄 许可证

ISC

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如需AI项目咨询服务，请访问 [咨询页面](/consulting)。

---

**构建时间**: 2026-02-08  
**最新测试**: [查看测试报告](./docs/TEST_REPORT.md)  
**版本**: 1.0.0  
**状态**: ✅ 生产就绪 (通过所有功能测试)
